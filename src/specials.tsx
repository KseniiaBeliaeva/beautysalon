import * as React from 'react';

let specials = {
    name: 'Gift vouchers',
    deal: ''
}

const companyName = { name: "test" };

class Specials extends React.Component {
    render() {
        return (
            <h1 className="header">{companyName.name}</h1>
        )
    };
}
export default Specials;













// ЭТО ПАПКА src/Services.jsx